server:
  port: 8083
spring:
  application:
    name: SLOT-MACHINE-GAME-SERVICE
  datasource:
    driverClassName: org.h2.Driver
    password: root
    username: root
    url: jdbc:h2:mem:slotMachine
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
